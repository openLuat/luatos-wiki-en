本库有专属demo，[点此链接查看camera的demo例子
This library has its own demo,[click this link to view the demo example of camera
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/camera/luat_lib_camera.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/camera/luat_lib_camera.c). If there is any error, please submit issue or help modify pr, thank you
参数
Parameters
成功返回true,否则返回
Returns true on success, otherwise
初始化后需要start才开始输出/扫码
After initialization, start is required to start output/scan code
初始化命令 见demo/camera/AIR105 注意:如扫码 camera初始化时需设置为灰度输出
See demo/camera/AIR105 for the initialization command. Note: If the scan code camera is initialized, it needs to be set to grayscale output.
初始化摄像头
Initialize camera
传入值类型|解释
Incoming Value Type | Explanation
多种类型 false 摄像头没有正常工作，true 拍照模式下拍照成功并保存完成， int 原始数据模式下本次返回的数据大小， string 扫码模式下扫码成功后的解码值
Various types of false cameras do not work normally, the true photo mode is successfully photographed and saved, the size of the data returned this time in int original data mode, and the decoded value after the code is successfully scanned in string scan mode.
返回值
Return Value
返回值类型|解释
return value type | explanation
高度
Height
关闭指定的camera，释放相应的IO资源
Close the specified camera and release the corresponding I/O resources.
回调方法
Callback Method
开始指定的
Start the specified
宽度
Width
例如
For example
例子
Examples
拍照
Take pictures
屏幕输出rgb图像
screen output rgb image
启动camera输出原始数据到用户的zbuff缓存区，输出1fps后会停止，并通过camera.on设置的回调函数回调接收到的长度，如果需要再次输出，请调用
Start camera to output the original data to the user's zbuff buffer, stop after outputting 1fps, and call back the received length through the callback function set by camera.on. if you need to output again, please call
摄像头
Camera
事件名称
Event Name
输出路径，目前只能用虚拟串口
Output path, currently only virtual serial port can be used
输出视频流到
Output video stream
停止指定的
Stop the specified
文件保存路径，空则写在上次路径里，默认是
File save path, empty is written in the last path, the default is
无返回值
No return value
写0, camera 1写
Write 0, camera 1 write
压缩质量，1最差，占用空间小，3最高，占用空间最大而且费时间，默认
Compression quality, 1 worst, small footprint, 3 highest, largest footprint and time-consuming, default
已适配
Adapted
用于存放数据的缓存区，大小必须不小于
The buffer area used to store data. The size must be no less
再次启动camera输出原始数据到用户的zbuff缓存区，输出1fps后会停止，并通过camera.on设置的回调函数回调接收到的长度，如果需要再次输出，请继续调用本
Start camera again to output the original data to the user's zbuff buffer, stop after outputting 1fps, and call back the received length through the callback function set by camera.on. if you need to output again, please continue to call this
注册摄像头事件回调
Register camera event callback