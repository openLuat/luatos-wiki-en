本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/sfd/luat_lib_sfd.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/sfd/luat_lib_sfd.c). If there is any error, please submit issue or help modify pr, thank you
擦除数据
Erase data
参数
Parameters
操作库
Operation Library
成功返回一个数据结构,否则返回
Returns a data structure on success, otherwise
成功返回true,失败返回
Returns true on success and true on failure
初始化
Initialization
传入值类型|解释
Incoming Value Type | Explanation
的片选脚对应的GPIO, 当类型是spi时才需要传
GPIO corresponding to the chip selection pin of, only needs to be transmitted when the type is spi
读取数据
Read data
读取长度,当前限制在256以内
Read length, currently limited to 256
返回的数据结构
returned data structure
返回值
Return Value
返回值类型|解释
return value type | explanation
检查spi flash状态
check spi flash status
类型, 可以是"spi", 也可以是"zbuff", 或者
type, which can be "spi" or "zbuff", or
例子
Examples
起始偏移量
Start Offset
数据
Data
写入数据
Write Data
芯片唯一
The only chip.
需要写入的数据,当前支持256字节及以下
The data to be written. Currently, 256 bytes or less are supported.
已适配
Adapted
这块区域通常是fdb/fskv库所在的区域, 所以不要混着用
This area is usually the area where the fdb/fskv library is located, so don't mix it up.
之后的固件支持onchip类型, 支持直接读写片上flash的一小块区域,一般是
After that, the firmware supports the onchip type and supports direct reading and writing of a small area of flash on the chip, which is generally
状态值, 0 未初始化成功,1初始化成功且空闲,2正忙
Status value, 0 not initialized successfully, 1 initialized successfully and idle, 2 busy
字节(64bit)的芯片
Byte (64bit) chip
总线的id, 或者 zbuff实例
the id of the bus, or the zbuff instance