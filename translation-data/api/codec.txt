从文件中解析出原始音频数据，比如从MP3文件里解析出PCM数据，这里的文件路径已经在codec.info传入，不需要再次传入
Parse the original audio data from the file, for example, parse the PCM data from the MP3 file. The file path here has already been passed in to codec.info and does not need to be passed in again.
从文件中解析出音频信息
Parsing audio information from a file
传入值类型|解释
Incoming Value Type | Explanation
例子
Examples
最少解码出多少字节的音频数据,默认
How many bytes of audio data are decoded at least, by default
创建一个amr-wb的encoder，编码等级默认
Create a amr-wb encoder with the default encoding level
创建一个amr的
Create an AMR
创建一个mp3的
Create an mp3
创建的编解码用的
Used for the created codec
创建编码器
Create Encoder
创建编解码用的
Create a codec
创建解码器
Create Decoder
参数
Parameters
声音通道数
Number of sound channels
多媒体-编解码
Multimedia-Codec
多媒体类型，目前支持
multimedia type, currently supported
大内存设备
Large Memory Device
存放输出数据的zbuff，空间必须不少于
Zbuff for storing output data, space must be no less
小内存设备
Small Memory Device
已适配
Adapted
常量
Constant
常量|类型|解释
constant | type | explanation
成功返回true,失败返回
Returns true on success and true on failure
成功返回一个数据结构,否则返回
Returns a data structure on success, otherwise
文件路径
File Path
无
None
是否成功解析
Whether the resolution was successful
是否是解码器，true解码器，false编码器，默认true，是解码器
Is it a decoder, true decoder, false encoder, default true, is a decoder
是否有符号
Is there a symbol
本库有专属demo，[点此链接查看codec的demo例子
This library has its own demo,[click this link to view the demo example of codec
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/multimedia/luat_lib_multimedia_codec.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/multimedia/luat_lib_multimedia_codec.c). If there is any error, please submit issue or help modify pr, thank you
格式
Format
格式，一般意义上的
format, in a general sense
的编码等级 0~7(即 MR475~MR122)值越大消耗的空间越多,音质越高,默认
The larger the encoding level 0 to 7 (that is, MR475 to MR122), the more space is consumed and the higher the sound quality is. The default
的编码等级 0~7(即 MR475~MR122)值越大消耗的空间越多,音质越高,默认0 amr_wb的编码等级 0~8,值越大消耗的空间越多,音质越高,默认
The larger the encoding level of 0 to 7 (that is, MR475 to MR122), the more space consumed and the higher the sound quality. The default encoding level of 0 amr_wb is 0 to 8. The larger the value, the more space consumed and the higher the sound quality. The default
编码等级，部分bsp有内部编码器，可能需要提前输入编码等级，比如air780ep的内部amr编码器
encoding level. some bsps have internal encoders. you may need to enter the encoding level in advance, such as the internal amr encoder of a air780ep.
编码音频数据，由于flash和ram空间一般比较有限，目前只支持amr-nb编码
encoding audio data, because flash and ram space is generally limited, currently only amr-nb encoding is supported.
编码音频数据，由于flash和ram空间一般比较有限，除了部分bsp有内部amr编码功能，目前只支持amr-nb编码
for encoding audio data, flash and ram space are generally limited, except that some bsps have internal amr encoding function, currently only amr-nb encoding is supported.
解码用的
for decoding
输入的数据,zbuff形式,从0到
input data, zbuff form, from 0
输出的数据,zbuff形式,自动添加到buff的尾部,如果空间大小不足,会自动扩展,但是会额外消耗时间,甚至会失败,所以尽量一开始就给足空间
The output data, in the form of zbuff, is automatically added to the tail of the buff. If the space is insufficient, it will automatically expand, but it will consume extra time and even fail, so try to give enough space at the beginning.
返回值
Return Value
返回值类型|解释
return value type | explanation
采样位数
Number of sampling bits
采样频率
Sampling frequency
释放编解码用的
For release codec
音频格式
Audio Format