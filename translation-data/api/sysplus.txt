本库是sys库的补充, 添加了如下内容
This library is a supplement to the sys library, adding the following content
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_sysplus_doc.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_sysplus_doc.c). If there is any error, please submit issue or help modify pr, thank you
参数
Parameters
超时时间，如果为nil，则表示无超时，永远等待
The timeout period. If it is nil, it means that there is no timeout and it waits forever.
成功返回table型的msg，超时返回
successful return table type msg, timeout return
成功返回true, 否则返回
Returns true on success, otherwise
传入值类型|解释
Incoming Value Type | Explanation
创建一个任务线程,在模块最末行调用该函数并注册模块中的任务函数,main.lua导入该模块即可
Create a task thread, call the function at the end of the module and register the task function in the module, and main.lua can import the module.
等待接收一个目标消息
Waiting to receive a target message
等待任务
Wait for task
返回该任务的线程号
Returns the thread number of the task
返回值
Return Value
返回值类型|解释
return value type | explanation
机制
Mechanism
接收到非目标消息时的回调函数
Callback function when non-target message is received
库的强力补充
A strong complement to the library
例子
Examples
目标消息，如果为nil，则表示接收到任意消息都会退出
The target message, if nil, indicates that any message received will exit
清除指定task的消息队列
clear the message queue of the specified task.
任务函数名,用于resume唤醒时调用
The name of the task function, which is called when the resume wakes up.
任务函数fun的可变参数
Variable parameters of the task function fun
任务名称
Task Name
任务名称,用于唤醒任务的
The task name, which wakes up the task
任务名称，用于唤醒任务的
The task name, which wakes up the task
任务消息机制, 即sub/pub的增强版本
Task message mechanism, an enhanced version of sub/pub
删除由taskInitEx创建的任务线程
Delete task threads created by taskInitEx
示例
Example
无
None
向目标任务发送一个消息
Send a message to the target task
向任务a,目标b发送消息
Send message to task a, target B
消息中的参数
Parameters in the message
消息中的参数1，同时也是waitMsg里的
The parameter 1 in the message is also in the waitMsg.
已适配
Adapted
在socket,libnet,http库等场景需要用到, 所以也需要
It is needed in socket,libnet,http library and other scenarios, so it is also needed.
注意, 本函数会自动注册成全局函数
Note that this function is automatically registered as a global function