报错信息列表
Error reporting information list
本库有专属demo，[点此链接查看http的demo例子
This library has its own demo,[click this link to view http demo examples
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/network/libhttp/luat_lib_http.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/network/libhttp/luat_lib_http.c). If there is any error, please submit issue or help modify pr, thank you
参数
Parameters
超时, 包括连接超时,读取数据超时
Timeouts, including connection timeouts, read data timeouts
传入值类型|解释
Incoming Value Type | Explanation
错误的响应体,通常是服务器问题
Bad response body, usually a server problem
错误的响应头部, 通常是服务器问题
Incorrect response header, usually a server problem
错误的状态, 一般是底层异常,请报
Error status, usually a bottom-level exception, please report
当code>100时, 代表服务器返回的头部数据
When 100, the header data returned on behalf of the server
地址, 支持 http和https, 支持域名, 支持自定义端口
Address, support http and https, support domain name, support custom port
额外配置 可选 包含 timeout:超时时间单位ms 可选,默认10分钟,写0即永久等待 dst:下载路径,可选 adapter:选择使用网卡,可选 debug:是否打开debug信息,可选,ipv6:是否为ipv6 默认不是,可选 callback:下载回调函数,参数 content_len:总长度 body_len:以下载长度 userdata 用户传参,可选 userdata:回调自定义传参
Additional configuration options include timeout: timeout unit ms is optional, default is 10 minutes, write 0 is permanent wait dst: download path, optional adapter: select to use network card, optional debug: whether to open debug information, optional, ipv6: whether to ipv6 default is not, optional callback: download callback function, parameter content_len: total length body_len: userdata user parameter by download length, optional userdata: callback custom parameter
返回值
Return Value
返回值类型|解释
return value type | explanation
服务器反馈的值>=100, 最常见的是200.如果是底层错误,例如连接失败, 返回值小于
The value of server feedback is> = 100, the most common is 200. If it is a low-level error, such as connection failure, the return value is less
服务器响应的内容字符串,如果是下载模式, 则返回文件大小
The content string of the server response, or the file size if it is in download mode
服务器ca证书数据, 可选, 一般不需要
server ca certificate data, optional, generally not required
功能报错,通常是更新包不合法
The function reports an error, usually because the update package is illegal.
接收数据报错, 网络问题
Receive data error, network problem
可选, 对POST/PUT等请求方式有效
Optional, valid for POST/PUT requests
客户端
Client
客户端私钥加密数据, 可选, 一般不需要, 双向https认证才需要
client private key encryption data, optional, generally not required, two-way https authentication is required
客户端私钥口令数据, 可选, 一般不需要, 双向https认证才需要
Client private key password data, optional, generally not required, two-way https authentication is required.
客户端ca证书数据, 可选, 一般不需要, 双向https认证才需要
client ca certificate data, optional, generally not required, two-way https authentication is required
例子
Examples
连接服务器失败, 未联网,地址错误,域名错误
Failed to connect to server, not networked, address error, domain name error
请求
Request
请求,但下载到文件
request, but download to file
请求方法, 支持 GET/POST 等合法的HTTP方法
The request method, which supports legitimate HTTP methods such as GET and POST.
请求头 可选 例如
The request header is optional such
使用http库,需要引入sysplus库, 且需要在task内使用
To use the http library, you need to introduce the sysplus library and use it in the task.
示例
Example
提前断开了连接, 网络或服务器问题
Disconnected prematurely, network or server issues
下载文件过程报错, 网络问题或下载路径问题
Error reported during file download, network problem or download path problem
已适配
Adapted
自定义超时时间
Custom Timeout