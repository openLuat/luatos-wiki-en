本库有专属demo，[点此链接查看ioqueue的demo例子
This library has its own demo,[click this link to view ioqueue demo examples
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/io_queue/luat_lib_io_queue.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/../components/io_queue/luat_lib_io_queue.c). If there is any error, please submit issue or help modify pr, thank you
参数
Parameters
初始化一个io操作队列
initialize an io operation queue
初始输出电平
Initial output level
传入值类型|解释
Incoming Value Type | Explanation
存放IO捕获数据的buff，按照1byte pin + 1byte level + 4byte tick形式存放数据
The buff used to store IO capture data. The data is stored in the form of 1byte pin 1byte level 4byte tick.
存放IO输入数据的buff，按照1byte pin + 1byte level 形式存放数据
The buff that stores IO input data, and stores data in the form of 1byte pin 1byte level
定时器最大计时时间 考虑到lua是int类型，最小0x10000, 最大值为0x7fffffff，默认为最大值
The maximum timer time of the timer takes into account that lua is of int type, with a minimum of 0x 10000 and a maximum of 0 x7fffffff, and the default is the maximum value.
队列是否执行完成
Queue execution completed
对io操作队列增加捕获一次IO状态命令
Add a capture IO status command to the IO operation queue
对io操作队列增加读取gpio命令
add the read gpio command to the io operation queue
对io操作队列增加结束捕获某个IO命令
Add the IO operation queue to end the capture of an IO command.
对io操作队列增加设置捕获某个IO命令
add settings to the io operation queue to capture a certain IO command
对io操作队列增加设置gpio命令
add the gpio command to the io operation queue
对io操作队列增加输出GPIO命令
Adding Output GPIO Commands to the IO Operation Queue
对io操作队列增加延时命令
add a delay command to the io operation queue
对io操作队列增加一次重复延时，在前面必须有setdelay且是连续延时
add a repeat delay to the I/o operation queue. the delay must be setdelay and continuous.
返回单次循环中已经执行的cmd次数，如果是0，可能是一次循环刚刚结束
Returns the number of cmds executed in a single loop. If it is 0, it may be that a loop has just ended.
返回多少组IO捕获数据
How many groups of IO capture data are returned
返回多少组IO输入数据
How many groups of IO input data are returned
返回已经循环的次数，如果是0，表示一次循环都没有完成
Returns the number of cycles that have been performed. If it is 0, it means that none of the cycles have been completed.
返回值
Return Value
返回值类型|解释
return value type | explanation
获取io操作队列中输入和捕获的数据
get the data entered and captured in the io operation queue
检测io操作队列是否已经执行完成
Check whether the io operation queue has been completed
开关，默认是false关
Switch, the default is false off
例子
Examples
启动/停止一个带系统tick返回的外部中断
Start/stop an external interrupt with a system tick return
启动io操作队列
start io operation queue
清空io操作队列
empty the io operation queue
上下拉模式,只能是
pull-up mode, can only be
设置成默认上下拉输出高电平
Set to default pull-down output high level
设置成上拉输入
Set to pull-up input
是否连续是连续延时，默认否，如果是，定时器在时间到后不会停止而是重新计时
Whether it is continuous is continuous delay, the default is no, if it is, the timer will not stop but time again after the time is up.
是否是输入
Whether input or not
释放io操作队列的资源，下次使用必须重新
release the resources of the I/o operation queue. the resource must be re-used next time.
输出电平
Output level
停止io操作队列，可以通过start从头开始
stop the io operation queue, you can start from the beginning
无
None
序列操作
Sequence operation
延时9us+15个tick,在之后遇到delay命令时，会延时9us+15个
The delay is 9us 15 ticks, and when the delay command is encountered later, the delay will be 9us 15 ticks.
延时时间
Delay time
延时微调时间,0~255tick,总的延时时间是
Delay fine tuning time, 0~255tick, the total delay time is
一个完整周期需要的命令，可以比实际的多
A full cycle requires more commands than the actual
已适配
Adapted
以timer0为时钟源初始化一个io操作队列，有10个有效命令，循环5次
Initialize an io operation queue with timer0 as the clock source, with 10 valid commands, loop 5 times
硬件定时器
Hardware Timer
硬件定时器id，默认用0，根据实际MCU确定，air105为0~5，与pwm共用，同一个通道号不能同时为pwm和
Hardware timer id, default 0, according to the actual MCU, air105 is 0~5, shared with pwm, the same channel number cannot be both pwm and
中断模式,只能是
Break mode, can only be
重复次数,默认是1，如果写0则表示无限次数循环
The number of repetitions, the default is 1, if you write 0, it means an infinite number of loops.