不支持
Not supported
不支持文件下载
File download not supported
与http库的差异
Differences from the http library
也可以通过uart.tx等支持zbuff的函数转发出去
It can also be forwarded through uart.tx and other functions that support zbuff.
传入值类型|解释
Incoming Value Type | Explanation
例子
Examples
参数
Parameters
可选,打开调试日志,默认
Optional, open debug log, default
可选,文件上传,若存在本参数,会强制以multipart/form-data形式上传
Optional. File upload. If this parameter exists, the file is forced to be uploaded in the form of multipart/form-data.
可选,是否优先尝试ipv6地址,默认是
Optional. Whether to try the ipv6 address first. The default value is
可选,直接把文件内容作为body上传, 优先级高于body参数
Optional. Upload the file content directly as the body. The priority is higher than that of the body parameter.
可选,网络适配器编号, 默认是自动选
Optional, network adapter number, default is automatically selected
可选,自定义body参数, 字符串/zbuff/table均可, 但不能与files和forms同时存在
Optional. The custom body parameter can be the string/zbuff/table, but cannot exist with files and forms at the same time.
可选,自定义的额外
optional, custom extra
可选,表单参数,若存在本参数,如果不存在files,按application/x-www-form-urlencoded上传
Optional, form parameter, if this parameter exists, if files does not exist, upload by application/x-www-form-urlencoded
可选,读取服务器响应的超时时间,单位秒,默认
Optional. The timeout period for reading the server response, in seconds. Default value
可选,默认GET, 如果有body,files,forms参数,会设置成
Optional. By default, GET is set
响应码,服务器返回的状态码>=100, 若本地检测到错误,会返回<0的值
Response code, the status code returned by the server> = 100, if an error is detected locally, it will return a value of <0
已适配
Adapted
库的补充
Supplement to the library
必选, 目标
Required, Target
情况1, code >= 100 时, resp会是个table, 包含2个元素
Case 1, when code >= 100, resp will be a table containing 2 elements
执行HTTP请求
Execute HTTP request
探索http库的扩展
Explore extensions to the http library
是个
is a
服务器正常响应时返回结果,|否则是错误信息或者
When the server responds normally, the result is returned. Otherwise, it is an error message or
本库有专属demo，[点此链接查看httpplus的demo例子
This library has its own demo,[click this link to view httpplus demo examples
本库的目的
Purpose of this library
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/../script/libs/httpplus.lua)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/../script/libs/httpplus.lua). If there is any error, please submit issue or help modify pr, thank you
示例
Example
解决大文件上传的问题
Solve problems with large file uploads
请求参数介绍
Request parameter introduction
返回值
Return Value
返回值resp的说明
Description of the return value resp
返回值类型|解释
return value type | explanation
通过query函数可以转为lua的
The query function can be used to convert
额外的待上传参数
Additional parameters to be uploaded