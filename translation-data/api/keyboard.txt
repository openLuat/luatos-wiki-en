为按下, 0 为 释放
is pressed, 0 is released
传入值类型|解释
Incoming Value Type | Explanation
使用 keyboard0~4, 二进制为 11111,  对应conf的十六进制表达为
Using keyboard0 ~ 4, the binary is 11111, and the hexadecimal expression of the corresponding conf is
使用 keyboard0~7, 对应conf为
Using keyboard0 ~ 7, the corresponding conf is
例子
Examples
做一个 2*3 键盘矩阵, 使用 keyboard0~4, 其中0~1做输入, 2~4做输出
Make a 2*3 keyboard matrix and use keyboard0 ~ 4, where 0~1 is input and 2~4 is output.
做一个 4*4 键盘矩阵, 使用 keyboard0~7, 其中0~3做输入, 4~7做输出
Make a 4*4 keyboard matrix and use keyboard0 ~ 7, where 0~3 is input and 4~7 is output.
其中0~1做输入, 2~4做输出, 二进制为 11100 对应map 为
Where 0~1 is input, 2~4 is output, and binary is 11100 corresponding to map
其中0~3做输入, 4~7做输出, 对应map 为
Where 0~3 is input and 4~7 is output, corresponding to map
初始化键盘矩阵
Initialize keyboard matrix
参数
Parameters
启用的keyboard管脚掩码, 例如使用keyboard0~9, 则掩码为 0x1FF, 若使用 0~3 则
The keyboard pin mask that is enabled. For example, if keyboard0 ~ 9 is used, the mask is 0x1FF. If 0~3 is used, the mask is 0x1FF.
已适配
Adapted
当前固定为0, 可以无视
Currently fixed to 0, can be ignored
无
None
本库有专属demo，[点此链接查看keyboard的demo例子
This library has its own demo,[click this link to view keyboard demo examples
本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_keyboard.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_keyboard.c). If there is any error, please submit issue or help modify pr, thank you
消抖配置,预留,可以不填
Shake elimination configuration, reserved, can not fill in
管脚方向映射, 其中输入为0,输出为1, 按位设置.  例如 keyboard0~3作为输入, keyboard4~7为输入, 则
Pin direction mapping, where the input is 0 and the output is 1, set by bit. For example, keyboard0 ~ 3 as input and keyboard4 ~ 7 as input, then
详细介绍
Detailed introduction
返回值
Return Value
适配状态未知
Adaptation status unknown
键盘矩阵
Keyboard Matrix
需要配合init的map进行解析
Need to cooperate with init map for parsing
预留, 当前填
Reserved, currently filled