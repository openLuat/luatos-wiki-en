本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_sdio.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_sdio.c). If there is any error, please submit issue or help modify pr, thank you
本sdio库挂载tf卡到文件系统功能已经被fatfs的sdio模式取代
The function of mounting the tf card to the file system in this sdio library has been replaced by the sdio mode of fatfs.
本sdio库仅保留直接读写TF卡的函数
This sdio library only retains functions that directly read and write TF cards.
参数
Parameters
初始化
Initialization
初始化sdio并直接读取sd卡数据
initialize sdio and read sd card data directly
传入值类型|解释
Incoming Value Type | Explanation
打开结果
Open Results
待写入的数据,长度必须是512的倍数
The length of the data to be written must be a multiple of 512.
返回值
Return Value
返回值类型|解释
return value type | explanation
挂载完成后, 使用 io 库的相关函数来操作就行
After the mount is completed, just use the relevant functions of the io library to operate it.
例如 使用 sdio 0 挂载TF卡
For example using sdio 0 to mount the TF card
例子
Examples
偏移量,必须是512的倍数
Offset, must be a multiple of 512
若读取成功,返回字符串,否则返回
If the read is successful, return string, otherwise return
若读取成功,返回true,否则返回
If the read is successful, return true, otherwise return
示例
Example
通道id,与具体设备有关,通常从0开始,默认
The channel ID, which is related to the specific device, usually starts from 0, the default
无
None
已适配
Adapted
长度,必须是512的倍数
Length, must be a multiple of 512
直接读写sd卡上的数据
direct reading and writing of data on sd card
直接写sd卡
write sd card directly
总线
Bus