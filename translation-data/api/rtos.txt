本页文档由[这个文件](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_rtos.c)自动生成。如有错误，请提交issue或帮忙修改后pr，谢谢
This page document is automatically generated by [this file](https://gitee.com/openLuat/LuatOS/tree/master/luat/modules/luat_lib_rtos.c). If there is any error, please submit issue or help modify pr, thank you
参数
Parameters
超时时长,单位毫秒
Timeout duration, in milliseconds
超时时长,通常是-1,永久等待
Timeout duration, usually -1, waiting permanently
传入值类型|解释
Incoming Value Type | Explanation
打印底层描述信息
Print the underlying description information
打印内存占用
Print memory footprint
当前已使用的内存大小,单位字节
Size of memory currently used, in bytes
底层操作库
underlying operation library
底层描述信息
underlying description information
定时器
Timer
读取版本号
Read version number
返回底层描述信息,格式为 LuatOS_$VERSION_$BSP,可用于OTA升级判断底层信息
Returns the underlying description in the format of LuatOS_$VERSION_$BSP, which can be used to determine the underlying information for OTA upgrades.
返回值
Return Value
返回值类型|解释
return value type | explanation
固件版本号,例如
Firmware version number, for example
固件编译日期
Firmware Compile Date
挂载sd卡或者spiflash后
mount sd card or after spiflash
关闭并释放一个定时器
Turn off and release a timer
获取编译日期
Get Compile Date
获取固件版本号
Get the firmware version number
获取固件编译日期
Get Firmware Compile Date
获取内存信息
Get memory information
获取硬件bsp型号
get hardware bsp model
将搜索并加载 /sdcard/user/sd_user_main.luac 和
will search and load/sdcard/user/sd_user_main.luac and
接受并处理底层消息队列
Accept and process the underlying message queue
进入待机模式
Enter standby mode
进入待机模式, 仅部分设备可用, 本API已废弃, 推荐使用pm库
Enter standby mode, only some devices are available, this API has been abandoned, recommend use pm library
空函数,什么都不做
Empty function, do nothing
历史最高已使用的内存大小,单位字节
Maximum memory used in history, in bytes
例子
Examples
路径A, 例如 "/sdcard/%s.luac",若不传值,将默认为"",另外,最大长度不能超过23字节
Path A, such as "/sdcard/%s.luac", defaults to "" if no value is passed. In addition, the maximum length cannot exceed 23 bytes.
路径B, 例如
Path B, for example
路径C, 例如
Path C, for example
路径D, 例如
Path D, for example
没有参数,没有返回值,没有逻辑处理
No parameters, no return value, no logical processing
内存自动收集配置，是lua本身收集机制的一种补充，不是必要的，而且只在luavm空闲时触发
automatic memory collection configuration is a supplement to lua's own collection mechanism. it is not necessary and is only triggered when luavm is idle
启动一个3秒的循环定时器
Start a 3 second cycle timer
启动一个定时器
Start a timer
如果是定时器消息,会返回定时器消息id及附加信息, 其他消息由底层决定,不向lua层进行任何保证
If it is a timer message, the timer message id and additional information will be returned. Other messages are determined by the bottom layer and are not guaranteed to the lua layer.
设备重启
Device restart
设置自定义lua脚本搜索路径,优先级高于内置路径
Set a custom lua script search path with priority over the built-in path
它通常只会出现在性能测试的代码里, 因为它什么都不干
It usually only appears in the code of the performance test, because it does nothing.
无
None
无返回值
No return value
系统内存, "lua"虚拟机内存, 默认为lua虚拟机内存
System memory, "lua" virtual machine memory, default is lua virtual machine memory
休眠时长,单位毫秒
Sleep duration, in milliseconds
已适配
Adapted
硬件bsp型号
hardware bsp model
用户代码请使用
User code please use
在绝大多数情况下,不会遇到这个函数的调用
In the vast majority of cases, calls to this function will not be encountered.
这个函数单纯就是 lua -> c -> lua 走一遍
This function is simply lua -> c -> lua walk again
重复次数,默认是
The number of repetitions. The default value is
总内存大小,单位字节
Total memory size in bytes